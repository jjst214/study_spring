<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.reserve.mapper.PaymentMapper">
	<insert id="insertPayment">
		insert into payment(pno, rno, pkind, pbank) values(seq_pno.nextval, seq_rno.currval, #{pkind}, #{pbank})
	</insert>
</mapper>